import type { ReactNode } from "react";
import { ArrowLeft, ArrowRight, CheckCircle2 } from "lucide-react";
import { motion } from "framer-motion";
import { Stepper } from "./Stepper";
interface Props { title:string; description:string; steps:string[]; current:number; children:ReactNode; canBack:boolean; canNext:boolean; nextLabel?:string; onBack:()=>void; onNext:()=>void; saving?:boolean; }
export function OnboardingShell({title,description,steps,current,children,canBack,canNext,nextLabel="Continue",onBack,onNext,saving}:Props){return <div className="min-h-screen bg-[#f7f9fc] px-4 py-6 md:px-8"><div className="mx-auto grid min-h-[calc(100vh-3rem)] max-w-6xl overflow-hidden rounded-[28px] border border-[#e2e7ef] bg-white shadow-[0_30px_100px_rgba(32,52,82,0.12)] lg:grid-cols-[1fr_0.72fr]"><section className="flex flex-col p-6 md:p-10 lg:p-12"><div className="mb-10 flex items-center justify-between"><div className="text-xl font-bold tracking-[-0.04em]">naano<span className="text-[#2864f0]">.</span></div><span className="text-xs font-semibold text-[#8b93a2]">EN</span></div><Stepper steps={steps} current={current}/><div className="mx-auto flex w-full max-w-2xl flex-1 flex-col pt-10"><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-[#2864f0]">Step {current+1} of {steps.length}</p><h1 className="mt-3 text-3xl font-semibold tracking-[-0.04em] text-[#171d2b] md:text-4xl">{title}</h1><p className="mt-2 max-w-xl text-sm leading-6 text-[#737c8d]">{description}</p></div><motion.div key={current} initial={{opacity:0,x:14}} animate={{opacity:1,x:0}} transition={{duration:.25}} className="mt-8 flex-1">{children}</motion.div><div className="mt-10 flex items-center justify-between gap-3 border-t border-[#edf0f4] pt-5"><button type="button" disabled={!canBack||saving} onClick={onBack} className="inline-flex items-center gap-2 rounded-xl px-4 py-3 text-sm font-semibold text-[#6f7787] disabled:opacity-30"><ArrowLeft className="h-4 w-4"/>Back</button><button type="button" disabled={!canNext||saving} onClick={onNext} className="inline-flex min-w-36 items-center justify-center gap-2 rounded-xl bg-[#2864f0] px-5 py-3 text-sm font-semibold text-white shadow-[0_8px_22px_rgba(40,100,240,.2)] disabled:cursor-not-allowed disabled:opacity-45">{saving?"Saving…":nextLabel}<ArrowRight className="h-4 w-4"/></button></div></div></section><aside className="hidden bg-[radial-gradient(circle_at_50%_25%,#dceaff,transparent_45%),linear-gradient(180deg,#f4f8ff,#fff)] p-10 lg:flex lg:flex-col lg:justify-center"><div className="rounded-[28px] border border-white/80 bg-white/75 p-7 shadow-[0_24px_70px_rgba(49,89,150,.14)] backdrop-blur"><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[.15em] text-[#2864f0]"><CheckCircle2 className="h-4 w-4"/>Your marketplace profile</div><div className="mt-7 text-3xl font-semibold tracking-[-.04em] text-[#171d2b]">Build trust before the first campaign.</div><p className="mt-3 text-sm leading-6 text-[#697386]">Complete your profile once and keep your creator or brand workspace ready for matching, campaigns and collaboration.</p><div className="mt-8 space-y-3">{steps.map((step,index)=><div key={step} className={`flex items-center gap-3 rounded-xl px-3 py-2 text-sm ${index<=current?"bg-[#eef4ff] text-[#245bdc]":"text-[#8b93a2"}`}><span className="grid h-6 w-6 place-items-center rounded-full bg-white text-[11px] font-bold">{index+1}</span>{step}</div>)}</div></div></aside></div></div>}

import { motion } from "framer-motion";
import { Check } from "lucide-react";
interface Props { steps:string[]; current:number; }
export function Stepper({steps,current}:Props){return <div className="flex items-center gap-2">{steps.map((step,index)=>{const done=index<current;const active=index===current;return <div key={step} className="flex min-w-0 flex-1 items-center gap-2"><div className={`relative flex h-8 w-8 shrink-0 items-center justify-center rounded-full border text-xs font-semibold transition ${done?"border-[#2864f0] bg-[#2864f0] text-white":active?"border-[#2864f0] bg-white text-[#2864f0]":"border-[#d9dde5] bg-white text-[#9aa1af"}`}>{done?<Check className="h-4 w-4"/>:index+1}{active&&<motion.span layoutId="active-step" className="absolute inset-[-4px] rounded-full border border-[#2864f0]/20"/>}</div><span className={`hidden truncate text-xs font-medium sm:block ${active?"text-[#202124]":"text-[#8b93a2"}`}>{step}</span>{index<steps.length-1&&<div className="mx-1 h-px flex-1 overflow-hidden bg-[#e5e8ed]"><motion.div initial={false} animate={{width:index<current?"100%":"0%"}} className="h-full bg-[#2864f0]"/></div>}</div>})}</div>}

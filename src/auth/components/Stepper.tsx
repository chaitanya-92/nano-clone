import { motion } from "framer-motion";
import { Check } from "lucide-react";
interface Props { steps:string[]; current:number; }
export function Stepper({steps,current}:Props){return <div className="flex items-center">{steps.map((step,index)=>{const done=index<current;const active=index===current;return <div key={step} className="flex min-w-0 flex-1 items-center">{<div className="flex min-w-0 items-center gap-2"><div className={`relative grid h-8 w-8 shrink-0 place-items-center rounded-full border text-[11px] font-bold ${done?"border-[#2864f0] bg-[#2864f0] text-white":active?"border-[#2864f0] bg-white text-[#2864f0] shadow-[0_0_0_4px_rgba(40,100,240,.08)]":"border-[#dfe3ea] bg-white text-[#9aa1af"}`}>{done?<Check className="h-4 w-4"/>:index+1}</div><span className={`hidden max-w-28 truncate text-xs font-semibold sm:block ${active?"text-[#202635]":"text-[#9aa1af"}`}>{step}</span></div>}{index<steps.length-1&&<div className="mx-3 h-px flex-1 bg-[#e7eaf0]"><motion.div initial={false} animate={{scaleX:index<current?1:0}} style={{transformOrigin:"left"}} className="h-full bg-[#2864f0]"/></div>}</div>})}</div>}
